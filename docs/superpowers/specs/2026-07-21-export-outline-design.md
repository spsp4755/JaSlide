# Export and Outline Design

The editor preview resolves CSS variables stored in imported HTML templates and applies the resolved background, font, and text color to editable content. The export service logs only a serializable renderer status/detail so failures retain their original error.

The outline review keeps the existing three-slide generation rule visible. It adds a default content slide with one editable key point and disables approval until three valid slides remain.
